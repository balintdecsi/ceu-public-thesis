# Public summary / ETD summary

Public summary for the MSc Business Analytics capstone.

Boundary: this document may contain **only** information visible in `docs/project/client_final_presentation_june2026.html`.

Build from the repository root:

```bash
uv sync
source .venv/bin/activate
cd docs/project/public_summary
make
```

Output: `main.pdf`.
